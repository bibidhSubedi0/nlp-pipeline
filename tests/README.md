# Tests

Bibek's unit + integration tests go here.

Convention:
- `test_<module_name>.py` for unit tests of a single module's `process()` function
- `test_pipeline_integration.py` for full end-to-end orchestrator runs
